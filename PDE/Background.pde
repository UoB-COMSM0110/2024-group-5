public class BackgroundImage {
    // Initial horizontal position of the background image.
    public int background_start_cur_X = 0;
    // Initial vertical position of the background image.
    public int background_start_cur_Y = 0;
    // Speed of the background image moving horizontally.
    public float speedX = 2.5;
    // Vertical movement of the background is set to zero, meaning no vertical movement.
    public float speedY = 0;

    // Constructor to create a background image object.
    public BackgroundImage() {
    }
}
